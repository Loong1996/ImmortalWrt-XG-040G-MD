# 这些文件会被装进固件、或者在 Linux 上直接执行，行尾必须是 LF。
# 在 core.autocrlf=true 的 Windows 上检出时，没有这几行就会拿到 CRLF ——
# 仓库里存的一直是 LF，所以 CI 编出来的固件没问题，但本地建 src-link feed
# 编译时，符号链接指向的是工作区那份，CRLF 的 shell 脚本在路由器上跑不起来。

packages/** text eol=lf
scripts/** text eol=lf
*.sh text eol=lf

# 补丁文件保持原样，任何转换都可能让它打不上
*.patch -text
